{
  "name": "odin",
  "version": "0.1.0",
  "main": "index.js",
  "author": "viegasdom",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "start": "yarn run clean && yarn run build && node ./dist/index",
    "dev": "nodemon -e ts --exec \"yarn run start\""
  },
  "dependencies": {
    "@types/ws": "^7.2.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "node-pty": "^0.9.0",
    "systeminformation": "^5.3.1",
    "typescript": "^3.9.3",
    "ws": "^7.3.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "@types/express": "^4.17.6",
    "@types/node": "^14.0.5",
    "nodemon": "^2.0.4"
  },
  "prettier": {
    "printWidth": 120,
    "singleQuote": true,
    "trailingComma": "all"
  }
}
